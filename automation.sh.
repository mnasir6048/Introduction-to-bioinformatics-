#!/bin/bash

# 1. Create the CSV file and add the header row
echo "Name,Email,Slack_Username,Area_of_Interest" > output.csv

# 2. Run your Python script (Member 1)
python3 abdulrauf.py | tr '\n' ',' | sed 's/,$//' >> output.csv

# 3. Add a new line at the end
echo "" >> output.csv

echo "Done! Check the file named output.csv"

